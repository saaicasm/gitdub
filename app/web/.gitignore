# dependencies
node_modules

# build output
dist
dist-ssr
*.local

# vite
.vite

# typescript incremental builds
*.tsbuildinfo

# env (commit .env / .env.[mode] for shared defaults; never commit .local)
.env.local
.env.*.local

# logs
*.log
npm-debug.log*

# test coverage
coverage

# editor / OS
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.swp
